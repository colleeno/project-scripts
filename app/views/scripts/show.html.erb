<h2><%= @script.quote %></h2>
<p><%= @script.character %></p>
<p><%= @script.show %></p>

<% if @script.tags.length > 0 %>
  <p>tags</p>
  <% @script.tags.each do |tag| %>
    <%= tag.category.name %>
    <% end %>
<% end %>
