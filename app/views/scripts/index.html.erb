<h2>scripts</h2>
<div class="transitions-enabled masonry grid">
<% @scripts.each do |script| %>
  <div class= "grid-item"><p><%= link_to "#{script.quote}", script_path(script) %></p></div>
<% end %>
</div>
<%= link_to "add new quote", new_script_path %>
