<h2>Posts by Tag: <%= @category.name %></h2>

<% @scripts.each do |script| %>
    <h3><%= link_to "#{script.quote}", script_path(script) %></h3>
<% end %>

  <%= link_to "add new script", new_script_path %>
